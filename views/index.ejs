<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ShopWise — Honest Business Diagnostics for Local Shops</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <style>
    /* ─── Tokens ─────────────────────────────────────────────── */
    :root {
      --bg:        #F8F7F4;
      --surface:   #FFFFFF;
      --surface-2: #F2F0EB;
      --border:    #E3E1DB;
      --border-2:  #CCCAC3;

      --text-1: #0E0E0E;
      --text-2: #484540;
      --text-3: #8A877F;

      --high:        #B91C1C;
      --high-bg:     #FEF2F2;
      --high-border: #FECACA;
      --med:         #92400E;
      --med-bg:      #FFFBEB;
      --med-border:  #FDE68A;
      --good:        #14532D;
      --good-bg:     #F0FDF4;
      --good-border: #BBF7D0;

      --radius:    8px;
      --radius-lg: 14px;
      --shadow-sm: 0 1px 2px rgba(0,0,0,.06), 0 1px 3px rgba(0,0,0,.04);
      --shadow:    0 4px 16px rgba(0,0,0,.07);
      --shadow-lg: 0 8px 32px rgba(0,0,0,.09);
    }

    /* ─── Reset ──────────────────────────────────────────────── */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      font-family: 'Inter', system-ui, sans-serif;
      background: var(--bg); color: var(--text-1);
      line-height: 1.6; -webkit-font-smoothing: antialiased;
    }

    /* ─── Layout ─────────────────────────────────────────────── */
    .container  { max-width: 1120px; margin: 0 auto; padding: 0 40px; }
    .section    { padding: 100px 0; }
    .section-sm { padding: 72px 0; }

    /* ─── Nav ────────────────────────────────────────────────── */
    nav {
      position: sticky; top: 0; z-index: 100;
      background: rgba(248,247,244,.93);
      border-bottom: 1px solid var(--border);
      backdrop-filter: blur(12px);
    }
    .nav-inner {
      display: flex; align-items: center; justify-content: space-between;
      height: 64px;
    }
    .nav-brand {
      font-size: 17px; font-weight: 800; letter-spacing: -.03em;
      color: var(--text-1); text-decoration: none;
    }
    .nav-links { display: flex; align-items: center; gap: 28px; }
    .nav-link {
      font-size: 14px; font-weight: 500; color: var(--text-2);
      text-decoration: none; transition: color .15s;
    }
    .nav-link:hover { color: var(--text-1); }
    .nav-cta {
      display: inline-flex; align-items: center;
      background: var(--text-1); color: #fff;
      font-size: 13px; font-weight: 600;
      padding: 9px 18px; border-radius: 6px;
      text-decoration: none; transition: opacity .15s;
    }
    .nav-cta:hover { opacity: .88; }
    .nav-mobile-toggle {
      display: none; background: none; border: none;
      cursor: pointer; color: var(--text-2); padding: 4px;
    }
    /* mobile menu lives inside <nav> so it sticks with the bar */
    .mobile-menu {
      display: none; flex-direction: column;
      background: var(--surface);
      border-top: 1px solid var(--border);
      padding: 12px 24px 20px;
    }
    .mobile-menu.open { display: flex; }
    .mobile-menu a {
      color: var(--text-2); text-decoration: none;
      font-size: 15px; font-weight: 500;
      padding: 12px 0; border-bottom: 1px solid var(--border);
    }
    .mobile-menu a:last-child { border-bottom: none; }

    /* ─── Buttons ────────────────────────────────────────────── */
    .btn {
      display: inline-flex; align-items: center; justify-content: center; gap: 6px;
      font-family: inherit; font-weight: 600; font-size: 14px;
      border-radius: 7px; border: none; cursor: pointer;
      text-decoration: none; transition: all .15s; white-space: nowrap;
    }
    .btn-primary  { background: var(--text-1); color: #fff; padding: 13px 24px; }
    .btn-primary:hover { opacity: .88; }
    .btn-outline  { background: transparent; color: var(--text-1); padding: 12px 22px; border: 1.5px solid var(--border-2); }
    .btn-outline:hover { border-color: var(--text-1); }
    .btn-white    { background: #fff; color: var(--text-1); padding: 13px 24px; }
    .btn-white:hover { background: rgba(255,255,255,.92); }
    .btn-ghost-white {
      background: transparent; color: rgba(255,255,255,.7);
      border: 1.5px solid rgba(255,255,255,.2); padding: 13px 24px;
    }
    .btn-ghost-white:hover { border-color: rgba(255,255,255,.5); color: #fff; }
    .btn-lg { padding: 15px 28px; font-size: 15px; }

    /* ─── Status chips ───────────────────────────────────────── */
    .chip {
      display: inline-flex; align-items: center; gap: 5px;
      font-size: 12px; font-weight: 600; letter-spacing: .1px;
      padding: 3px 9px; border-radius: 5px;
    }
    .chip-high    { background: var(--high-bg);  color: var(--high);  border: 1px solid var(--high-border); }
    .chip-med     { background: var(--med-bg);   color: var(--med);   border: 1px solid var(--med-border); }
    .chip-good    { background: var(--good-bg);  color: var(--good);  border: 1px solid var(--good-border); }
    .chip-neutral { background: var(--surface-2); color: var(--text-2); border: 1px solid var(--border); }

    /* ─── Section labels ─────────────────────────────────────── */
    .label {
      font-size: 11px; font-weight: 700; letter-spacing: .8px;
      text-transform: uppercase; color: var(--text-3);
    }

    /* ─── Hero ───────────────────────────────────────────────── */
    .hero { padding: 88px 0 96px; background: var(--bg); }
    .hero-inner {
      display: grid; grid-template-columns: 1fr 400px;
      gap: 72px; align-items: start;
    }
    .hero-eyebrow { margin-bottom: 18px; }
    h1 {
      font-size: clamp(38px, 4.2vw, 60px);
      font-weight: 900; letter-spacing: -.04em;
      line-height: 1.06; color: var(--text-1);
    }
    .hero-sub {
      font-size: 17px; color: var(--text-2); line-height: 1.8;
      max-width: 500px; margin: 18px 0 32px;
    }

    /* form card */
    .hero-form-card {
      background: var(--surface);
      border: 1.5px solid var(--border);
      border-radius: var(--radius-lg);
      padding: 28px; box-shadow: var(--shadow);
    }
    .hero-form-card h3 {
      font-size: 15px; font-weight: 700; color: var(--text-1); margin-bottom: 20px;
    }
    .form-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 12px; }
    .form-field  { display: flex; flex-direction: column; gap: 5px; }
    .form-label  { font-size: 11px; font-weight: 700; letter-spacing: .4px; text-transform: uppercase; color: var(--text-3); }
    .form-input {
      padding: 10px 12px; border: 1.5px solid var(--border);
      border-radius: var(--radius); font-size: 14px;
      font-family: inherit; color: var(--text-1);
      background: var(--bg); outline: none; transition: border-color .15s;
    }
    .form-input:focus { border-color: var(--text-1); background: #fff; }
    select.form-input { cursor: pointer; }
    .form-note { font-size: 12px; color: var(--text-3); margin: 14px 0 18px; line-height: 1.6; }
    .form-submit { width: 100%; }

    /* hero preview card */
    .hero-preview {
      background: var(--surface); border: 1.5px solid var(--border);
      border-radius: var(--radius-lg); box-shadow: var(--shadow-lg);
      overflow: hidden; position: sticky; top: 80px;
    }
    .hp-header {
      padding: 18px 22px 14px;
      border-bottom: 1px solid var(--border);
    }
    .hp-eyebrow { font-size: 10px; font-weight: 700; letter-spacing: .7px; text-transform: uppercase; color: var(--text-3); margin-bottom: 6px; }
    .hp-shop    { font-size: 14px; font-weight: 700; color: var(--text-1); }
    .hp-loc     { font-size: 12px; color: var(--text-3); margin-top: 2px; }
    .hp-metrics { padding: 0 22px; border-bottom: 1px solid var(--border); }
    .hp-row {
      display: flex; align-items: center; justify-content: space-between;
      padding: 10px 0; border-bottom: 1px solid var(--border); font-size: 13px;
    }
    .hp-row:last-child { border-bottom: none; }
    .hp-row-label { color: var(--text-2); font-weight: 500; }
    .fixbar-wrap { display: flex; align-items: center; gap: 10px; }
    .fixbar { width: 80px; height: 5px; background: var(--border); border-radius: 99px; overflow: hidden; }
    .fixbar-fill { height: 100%; background: var(--text-1); border-radius: 99px; }
    .fixbar-pct  { font-size: 13px; font-weight: 800; color: var(--text-1); }
    .hp-diagnosis {
      padding: 14px 22px; background: #FFFDF8;
      border-bottom: 1px solid var(--border);
    }
    .hp-diag-label { font-size: 10px; font-weight: 700; letter-spacing: .6px; text-transform: uppercase; color: var(--text-3); margin-bottom: 7px; }
    .hp-diag-text  { font-size: 12px; color: var(--text-2); line-height: 1.65; }
    .hp-strategy { padding: 14px 22px; }
    .hp-strat-label { font-size: 10px; font-weight: 700; letter-spacing: .6px; text-transform: uppercase; color: var(--text-3); margin-bottom: 5px; }
    .hp-strat-value { font-size: 13px; font-weight: 700; color: var(--text-1); }

    /* ─── Problem section ────────────────────────────────────── */
    .problem { background: #111111; }
    .problem .label { color: rgba(255,255,255,.3); }
    .problem h2 {
      font-size: clamp(30px, 3.8vw, 52px);
      font-weight: 900; letter-spacing: -.04em;
      color: #fff; line-height: 1.1; margin: 16px 0 14px;
    }
    .problem-intro {
      font-size: 17px; color: rgba(255,255,255,.5); line-height: 1.8;
      max-width: 580px; margin-bottom: 56px;
    }
    .problem-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      border: 1px solid rgba(255,255,255,.08); border-radius: var(--radius-lg);
      overflow: hidden;
    }
    .problem-cell {
      padding: 32px 28px;
      background: rgba(255,255,255,.03);
      border-right: 1px solid rgba(255,255,255,.06);
      transition: background .2s;
    }
    .problem-cell:last-child { border-right: none; }
    .problem-cell:hover { background: rgba(255,255,255,.06); }
    .pc-icon  { font-size: 24px; margin-bottom: 14px; }
    .pc-title { font-size: 15px; font-weight: 700; color: #fff; margin-bottom: 8px; }
    .pc-desc  { font-size: 13px; color: rgba(255,255,255,.45); line-height: 1.65; }

    /* ─── Reality Check ──────────────────────────────────────── */
    .reality-check { background: var(--surface); }
    .rc-header { text-align: center; margin-bottom: 60px; }
    h2.section-title {
      font-size: clamp(30px, 3.8vw, 52px);
      font-weight: 900; letter-spacing: -.04em;
      line-height: 1.1; color: var(--text-1); margin: 14px 0 14px;
    }
    .section-sub {
      font-size: 17px; color: var(--text-2);
      max-width: 540px; margin: 0 auto; line-height: 1.8;
    }
    .rc-columns {
      display: grid; grid-template-columns: 1fr 1fr;
      border: 1.5px solid var(--border); border-radius: var(--radius-lg); overflow: hidden;
    }
    .rc-col { padding: 36px 32px; }
    .rc-col-right { background: var(--surface-2); border-left: 1.5px solid var(--border); }
    .rc-col-heading {
      font-size: 12px; font-weight: 700; letter-spacing: .4px;
      text-transform: uppercase; margin-bottom: 28px;
      display: flex; align-items: center; gap: 9px; color: var(--text-2);
    }
    .dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
    .dot-green { background: #22C55E; }
    .dot-red   { background: #EF4444; }
    .rc-item {
      display: flex; align-items: flex-start; gap: 14px;
      padding: 14px 0; border-bottom: 1px solid var(--border);
    }
    .rc-item:last-child { border-bottom: none; padding-bottom: 0; }
    .rc-icon {
      width: 34px; height: 34px; border-radius: 7px;
      display: flex; align-items: center; justify-content: center;
      font-size: 15px; flex-shrink: 0; margin-top: 1px;
    }
    .rc-icon.green { background: var(--good-bg); }
    .rc-icon.red   { background: var(--high-bg); }
    .rc-item-title { font-size: 14px; font-weight: 600; color: var(--text-1); margin-bottom: 3px; }
    .rc-item-desc  { font-size: 13px; color: var(--text-3); line-height: 1.55; }

    /* ─── Sample Report ──────────────────────────────────────── */
    .sample-report { background: var(--surface-2); }
    .sr-header { margin-bottom: 44px; }
    .sr-title { font-size: clamp(28px, 3.5vw, 48px); font-weight: 900; letter-spacing: -.04em; margin: 14px 0 12px; }
    .sr-sub   { font-size: 16px; color: var(--text-2); max-width: 500px; line-height: 1.75; }

    .report-card {
      background: var(--surface); border: 1.5px solid var(--border);
      border-radius: var(--radius-lg); box-shadow: var(--shadow); overflow: hidden;
    }
    .report-top {
      padding: 22px 28px; border-bottom: 1px solid var(--border);
      display: flex; align-items: flex-start; justify-content: space-between; gap: 16px;
    }
    .report-shop { font-size: 16px; font-weight: 700; color: var(--text-1); }
    .report-meta { font-size: 12px; color: var(--text-3); margin-top: 4px; }
    .report-scores {
      display: grid; grid-template-columns: repeat(3, 1fr);
      border-bottom: 1px solid var(--border);
    }
    .rs-cell {
      padding: 22px 24px;
      border-right: 1px solid var(--border);
    }
    .rs-cell:last-child { border-right: none; }
    .rs-label { font-size: 11px; font-weight: 700; letter-spacing: .5px; text-transform: uppercase; color: var(--text-3); margin-bottom: 10px; }
    .rs-value { font-size: 26px; font-weight: 800; color: var(--text-1); line-height: 1; margin-bottom: 8px; }
    .rs-note  { font-size: 12px; color: var(--text-3); line-height: 1.5; }
    .report-diagnosis {
      padding: 24px 28px; border-bottom: 1px solid var(--border); background: #FFFDF8;
    }
    .rd-label { font-size: 11px; font-weight: 700; letter-spacing: .5px; text-transform: uppercase; color: var(--text-3); margin-bottom: 10px; }
    .rd-text  { font-size: 15px; color: var(--text-1); line-height: 1.8; }
    .report-breakdown {
      display: grid; grid-template-columns: 1fr 1fr;
      border-bottom: 1px solid var(--border);
    }
    .rb-col { padding: 22px 24px; }
    .rb-col:first-child { border-right: 1px solid var(--border); }
    .rb-heading {
      font-size: 12px; font-weight: 700; text-transform: uppercase; letter-spacing: .4px;
      color: var(--text-2); margin-bottom: 16px; display: flex; align-items: center; gap: 8px;
    }
    .rb-row {
      display: flex; align-items: center; justify-content: space-between;
      padding: 9px 0; border-bottom: 1px solid var(--border); font-size: 13px;
    }
    .rb-row:last-child { border-bottom: none; }
    .rb-row-label { color: var(--text-2); }
    .rb-bar-group { display: flex; align-items: center; gap: 8px; }
    .rb-bar { width: 56px; height: 4px; background: var(--border); border-radius: 99px; overflow: hidden; }
    .rb-bar-fill { height: 100%; background: var(--text-1); border-radius: 99px; }
    .rb-score { font-size: 12px; font-weight: 700; color: var(--text-1); min-width: 24px; text-align: right; }
    .report-strategy {
      padding: 22px 28px;
      display: flex; align-items: flex-start; gap: 14px;
    }
    .rstr-label { font-size: 11px; font-weight: 700; letter-spacing: .5px; text-transform: uppercase; color: var(--text-3); margin-bottom: 6px; }
    .rstr-title { font-size: 15px; font-weight: 700; color: var(--text-1); margin-bottom: 6px; }
    .rstr-desc  { font-size: 14px; color: var(--text-2); line-height: 1.65; }

    /* ─── Algorithm section ──────────────────────────────────── */
    .algorithm { background: var(--surface); }
    .alg-header { text-align: center; margin-bottom: 52px; }
    .alg-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; }
    .alg-card {
      padding: 28px; border: 1.5px solid var(--border);
      border-radius: var(--radius-lg); background: var(--bg);
    }
    .alg-num {
      width: 34px; height: 34px; border-radius: 7px;
      background: var(--text-1); color: #fff;
      font-size: 15px; font-weight: 800;
      display: flex; align-items: center; justify-content: center; margin-bottom: 16px;
    }
    .alg-title { font-size: 14px; font-weight: 700; color: var(--text-1); margin-bottom: 8px; }
    .alg-desc  { font-size: 13px; color: var(--text-2); line-height: 1.65; }
    .alg-tags  { display: flex; flex-wrap: wrap; gap: 5px; margin-top: 14px; }
    .alg-tag {
      font-size: 11px; padding: 3px 8px; border-radius: 4px;
      background: var(--surface); border: 1px solid var(--border);
      color: var(--text-3); font-weight: 500;
    }

    /* ─── Pricing ────────────────────────────────────────────── */
    .pricing { background: var(--surface-2); }
    .pricing-header { text-align: center; margin-bottom: 44px; }
    .pricing-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
    .price-card {
      background: var(--surface); border: 1.5px solid var(--border);
      border-radius: var(--radius-lg); padding: 30px; position: relative;
    }
    .price-card.featured { border-color: var(--text-1); }
    .featured-tag {
      position: absolute; top: -12px; left: 24px;
      background: var(--text-1); color: #fff;
      font-size: 11px; font-weight: 700; letter-spacing: .2px;
      padding: 4px 12px; border-radius: 4px;
    }
    .plan-name  { font-size: 16px; font-weight: 700; color: var(--text-1); margin-bottom: 6px; }
    .plan-desc  { font-size: 14px; color: var(--text-2); line-height: 1.6; }
    .plan-price { margin: 22px 0; padding: 18px 0; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
    .price-amt  { font-size: 34px; font-weight: 800; color: var(--text-1); line-height: 1; }
    .price-per  { font-size: 13px; color: var(--text-3); margin-top: 5px; }
    .plan-features { list-style: none; }
    .plan-features li {
      font-size: 13px; color: var(--text-2); padding: 8px 0;
      border-bottom: 1px solid var(--border);
      display: flex; align-items: flex-start; gap: 10px;
    }
    .plan-features li:last-child { border-bottom: none; }
    .plan-features li::before { content: '—'; color: var(--text-3); flex-shrink: 0; }
    .plan-features li.dim { color: var(--text-3); }
    .plan-cta { margin-top: 22px; width: 100%; justify-content: center; }

    /* ─── CTA Banner ─────────────────────────────────────────── */
    .cta-banner { background: #111111; padding: 96px 0; text-align: center; }
    .cta-banner .label { color: rgba(255,255,255,.3); display: block; margin-bottom: 18px; }
    .cta-banner h2 {
      font-size: clamp(28px, 3.8vw, 50px); font-weight: 900;
      letter-spacing: -.04em; color: #fff; line-height: 1.1;
    }
    .cta-banner p { font-size: 17px; color: rgba(255,255,255,.5); max-width: 500px; margin: 16px auto 40px; line-height: 1.8; }
    .cta-actions { display: flex; align-items: center; justify-content: center; gap: 12px; }

    /* ─── Footer ─────────────────────────────────────────────── */
    footer { background: #0A0A0A; padding: 56px 0 36px; }
    .footer-grid {
      display: grid; grid-template-columns: 2fr 1fr 1fr; gap: 48px;
      padding-bottom: 40px; border-bottom: 1px solid rgba(255,255,255,.07);
    }
    .footer-brand { font-size: 16px; font-weight: 800; color: #fff; letter-spacing: -.02em; }
    .footer-desc  { font-size: 13px; color: rgba(255,255,255,.3); margin-top: 12px; max-width: 260px; line-height: 1.7; }
    .footer-col h4 { font-size: 11px; font-weight: 700; letter-spacing: .6px; text-transform: uppercase; color: rgba(255,255,255,.3); margin-bottom: 16px; }
    .footer-col ul { list-style: none; }
    .footer-col li { margin-bottom: 10px; }
    .footer-col a  { font-size: 14px; color: rgba(255,255,255,.45); text-decoration: none; transition: color .15s; }
    .footer-col a:hover { color: rgba(255,255,255,.85); }
    .footer-bottom { display: flex; justify-content: space-between; padding-top: 28px; font-size: 13px; color: rgba(255,255,255,.22); }

    /* ─── Responsive ─────────────────────────────────────────── */
    @media (max-width: 1024px) {
      .hero-inner { grid-template-columns: 1fr; gap: 48px; }
      .hero-preview { position: static; max-width: 480px; }
      .alg-grid { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 768px) {
      .container { padding: 0 20px; }
      .section   { padding: 72px 0; }

      .nav-links { display: none; }
      .nav-mobile-toggle { display: flex; }

      .form-grid-2 { grid-template-columns: 1fr; }

      .problem-grid { grid-template-columns: 1fr; }
      .problem-cell { border-right: none; border-bottom: 1px solid rgba(255,255,255,.06); }
      .problem-cell:last-child { border-bottom: none; }

      .rc-columns { grid-template-columns: 1fr; }
      .rc-col-right { border-left: none; border-top: 1.5px solid var(--border); }

      .report-scores { grid-template-columns: 1fr 1fr; }
      .report-breakdown { grid-template-columns: 1fr; }
      .rb-col:first-child { border-right: none; border-bottom: 1px solid var(--border); }
      .report-top { flex-direction: column; }

      .alg-grid { grid-template-columns: 1fr; }
      .pricing-grid { grid-template-columns: 1fr; }

      .footer-grid { grid-template-columns: 1fr; gap: 28px; }
      .footer-bottom { flex-direction: column; gap: 10px; }
      .cta-actions { flex-direction: column; align-items: stretch; }
    }
    @media (max-width: 480px) {
      .report-scores { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>

<!-- ─── NAV ─────────────────────────────────────────────────────── -->
<nav>
  <div class="container nav-inner">
    <a href="/" class="nav-brand">ShopWise</a>
    <div class="nav-links">
      <a href="/sample/overview" class="nav-link">Sample report</a>
      <a href="/pricing" class="nav-link">Pricing</a>
      <a href="/#analyze" class="nav-cta">Run a Reality Check</a>
    </div>
    <button class="nav-mobile-toggle" id="burgerBtn" aria-label="Open menu">
      <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
        <line x1="2" y1="6" x2="20" y2="6"/><line x1="2" y1="11" x2="20" y2="11"/><line x1="2" y1="16" x2="20" y2="16"/>
      </svg>
    </button>
  </div>
  <div class="mobile-menu" id="mobileMenu">
    <a href="/sample/overview">Sample report</a>
    <a href="/pricing">Pricing</a>
    <a href="/#analyze">Run a Reality Check</a>
  </div>
</nav>

<!-- ─── HERO ─────────────────────────────────────────────────────── -->
<section class="hero">
  <div class="container hero-inner">

    <!-- Left: copy + form -->
    <div>
      <div class="label hero-eyebrow">Local business diagnostics</div>
      <h1>Find out what's fixable — and what's not.</h1>
      <p class="hero-sub">
        ShopWise analyzes local demand, competition, online visibility, and business basics to give small shops an honest diagnosis — not just marketing advice.
      </p>

      <form id="analyze" action="/analyze" method="POST">
        <div class="hero-form-card">
          <h3>Run a Reality Check</h3>
          <div class="form-grid-2">
            <div class="form-field">
              <label class="form-label">Business name</label>
              <input class="form-input" type="text" name="shopName" placeholder="e.g. Golden Bowl Noodles" required>
            </div>
            <div class="form-field">
              <label class="form-label">Business type</label>
              <select class="form-input" name="category">
                <option value="restaurant">Restaurant</option>
                <option value="cafe">Café / Coffee</option>
                <option value="bakery">Bakery</option>
                <option value="retail">Retail store</option>
                <option value="bar">Bar / Pub</option>
                <option value="other">Other</option>
              </select>
            </div>
          </div>
          <div class="form-grid-2">
            <div class="form-field">
              <label class="form-label">City or neighbourhood</label>
              <input class="form-input" type="text" name="address" placeholder="e.g. Financial District, SF" required>
            </div>
            <div class="form-field">
              <label class="form-label">Google rating <span style="font-weight:400;text-transform:none;letter-spacing:0">(optional)</span></label>
              <input class="form-input" type="number" name="rating" placeholder="e.g. 4.2" min="1" max="5" step="0.1">
            </div>
          </div>
          <div class="form-grid-2">
            <div class="form-field">
              <label class="form-label">No. of Google reviews <span style="font-weight:400;text-transform:none;letter-spacing:0">(optional)</span></label>
              <input class="form-input" type="number" name="reviews" placeholder="e.g. 89">
            </div>
            <div class="form-field">
              <label class="form-label">Website URL <span style="font-weight:400;text-transform:none;letter-spacing:0">(optional)</span></label>
              <input class="form-input" type="url" name="website" placeholder="https://...">
            </div>
          </div>
          <div class="form-note">No account or credit card required. We generate a diagnostic report from your inputs and public data signals.</div>
          <button type="submit" class="btn btn-primary form-submit">Run a Reality Check →</button>
        </div>
      </form>
    </div>

    <!-- Right: preview card -->
    <div>
      <div class="hero-preview">
        <div class="hp-header">
          <div class="hp-eyebrow">Reality Check Report · Sample</div>
          <div class="hp-shop">🍜 Golden Bowl Noodles</div>
          <div class="hp-loc">Restaurant · Financial District, San Francisco</div>
        </div>
        <div class="hp-metrics">
          <div class="hp-row">
            <span class="hp-row-label">Market Pressure</span>
            <span class="chip chip-high">High</span>
          </div>
          <div class="hp-row">
            <span class="hp-row-label">Fixability Score</span>
            <div class="fixbar-wrap">
              <div class="fixbar"><div class="fixbar-fill" style="width:58%"></div></div>
              <span class="fixbar-pct">58%</span>
            </div>
          </div>
          <div class="hp-row">
            <span class="hp-row-label">Competition Saturation</span>
            <span class="chip chip-high">High</span>
          </div>
          <div class="hp-row">
            <span class="hp-row-label">Local Demand</span>
            <span class="chip chip-high">Declining</span>
          </div>
          <div class="hp-row">
            <span class="hp-row-label">Online Visibility</span>
            <span class="chip chip-med">Weak</span>
          </div>
        </div>
        <div class="hp-diagnosis">
          <div class="hp-diag-label">Diagnosis</div>
          <div class="hp-diag-text">Around 58% of your challenges appear fixable through business actions. The area shows weak demand and high saturation — structural pressures outside your control.</div>
        </div>
        <div class="hp-strategy">
          <div class="hp-strat-label">Recommended strategy</div>
          <div class="hp-strat-value">Fix basics + Differentiate</div>
        </div>
      </div>
    </div>

  </div>
</section>

<!-- ─── PROBLEM ───────────────────────────────────────────────────── -->
<section class="section problem" id="problem">
  <div class="container">
    <div class="label">The honest problem</div>
    <h2>"Improve your SEO.<br>Post more on Instagram.<br>Ask for more reviews."</h2>
    <p class="problem-intro">
      That's the advice most tools give. And sometimes it helps. But often the harder truth is weak local demand, too many competitors, or a location that is structurally difficult. Spending on marketing before knowing which problem you have is an expensive guess.
    </p>
    <div class="problem-grid">
      <div class="problem-cell">
        <div class="pc-icon">📉</div>
        <div class="pc-title">The market may be the problem</div>
        <div class="pc-desc">If demand in your area is declining — due to office vacancies, population shifts, or economic pressure — no amount of marketing will reverse it. Knowing this changes everything.</div>
      </div>
      <div class="problem-cell">
        <div class="pc-icon">🏘️</div>
        <div class="pc-title">Saturation is real</div>
        <div class="pc-desc">Some neighbourhoods have far more restaurants than the area can support. Understanding your competitive density is the starting point, not an afterthought.</div>
      </div>
      <div class="problem-cell">
        <div class="pc-icon">🔧</div>
        <div class="pc-title">Some things are genuinely fixable</div>
        <div class="pc-desc">Your Google profile, review response rate, menu layout, website, and opening hours can all be improved — and they directly affect whether customers find and choose you.</div>
      </div>
    </div>
  </div>
</section>

<!-- ─── REALITY CHECK ─────────────────────────────────────────────── -->
<section class="section reality-check" id="reality-check">
  <div class="container">
    <div class="rc-header">
      <div class="label">Reality Check</div>
      <h2 class="section-title">Some problems are fixable.<br>Some are structural.</h2>
      <p class="section-sub">ShopWise separates what you can act on this week from what the market is doing to you.</p>
    </div>

    <div class="rc-columns">

      <!-- Left: Controllable -->
      <div class="rc-col">
        <div class="rc-col-heading"><span class="dot dot-green"></span> Things you control</div>

        <div class="rc-item">
          <div class="rc-icon green">📸</div>
          <div>
            <div class="rc-item-title">Google Maps photos & profile</div>
            <div class="rc-item-desc">Profiles with 10+ photos get significantly more clicks. Adding photos costs nothing and takes an hour.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon green">⭐</div>
          <div>
            <div class="rc-item-title">Review quality & response rate</div>
            <div class="rc-item-desc">Responding to reviews improves local search ranking and signals trust to potential customers.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon green">🍽️</div>
          <div>
            <div class="rc-item-title">Menu clarity & structure</div>
            <div class="rc-item-desc">A well-organised menu with clear sections and photos reduces decision fatigue and increases average order value.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon green">🌐</div>
          <div>
            <div class="rc-item-title">Website & delivery page quality</div>
            <div class="rc-item-desc">Missing opening hours, no order button, or slow mobile load all push customers to competitors.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon green">🎯</div>
          <div>
            <div class="rc-item-title">Positioning & differentiation</div>
            <div class="rc-item-desc">A clear reason to choose you over the restaurant next door can be defined and communicated — it is not fixed by the market.</div>
          </div>
        </div>
      </div>

      <!-- Right: External -->
      <div class="rc-col rc-col-right">
        <div class="rc-col-heading"><span class="dot dot-red"></span> Things the market controls</div>

        <div class="rc-item">
          <div class="rc-icon red">📊</div>
          <div>
            <div class="rc-item-title">Local spending power & confidence</div>
            <div class="rc-item-desc">Economic downturns and rising costs reduce how often people dine out. This is not something you can market your way out of.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon red">🏘️</div>
          <div>
            <div class="rc-item-title">Number of nearby competitors</div>
            <div class="rc-item-desc">If 14 similar restaurants are within 500m, your share of a fixed pool of customers is structurally constrained.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon red">🚶</div>
          <div>
            <div class="rc-item-title">Area foot traffic</div>
            <div class="rc-item-desc">Office vacancies, construction, or neighbourhood decline reduce the number of people walking past your door each day.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon red">📅</div>
          <div>
            <div class="rc-item-title">Tourism seasonality</div>
            <div class="rc-item-desc">Tourist-dependent areas see sharp demand swings that no amount of marketing can fully offset in low season.</div>
          </div>
        </div>
        <div class="rc-item">
          <div class="rc-icon red">💸</div>
          <div>
            <div class="rc-item-title">Rent, ingredient & energy costs</div>
            <div class="rc-item-desc">External cost pressure squeezes margins regardless of how well you market or how good your food is.</div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- ─── SAMPLE REPORT ─────────────────────────────────────────────── -->
<section class="section sample-report" id="sample">
  <div class="container">
    <div class="sr-header">
      <div class="label">Sample diagnostic report</div>
      <div class="sr-title">What an honest diagnosis looks like</div>
      <p class="sr-sub">Not generic tips — a specific assessment of what's fixable and what is structural market pressure.</p>
    </div>

    <div class="report-card">

      <div class="report-top">
        <div>
          <div class="report-shop">🍜 Golden Bowl Noodles</div>
          <div class="report-meta">Restaurant · Financial District, San Francisco · Diagnostic report · May 2026</div>
        </div>
        <a href="/sample/overview" class="btn btn-outline" style="font-size:13px;padding:9px 16px;flex-shrink:0;">View full report →</a>
      </div>

      <div class="report-scores">
        <div class="rs-cell">
          <div class="rs-label">Market Pressure</div>
          <div class="rs-value"><span class="chip chip-high" style="font-size:14px;padding:5px 12px;">High</span></div>
          <div class="rs-note">14 similar restaurants within 500m · Office vacancy ~28%</div>
        </div>
        <div class="rs-cell">
          <div class="rs-label">Fixability Score</div>
          <div class="rs-value">58%</div>
          <div class="rs-note">Around 58% of challenges appear actionable through business changes</div>
        </div>
        <div class="rs-cell">
          <div class="rs-label">Online Visibility</div>
          <div class="rs-value"><span class="chip chip-med" style="font-size:14px;padding:5px 12px;">Weak</span></div>
          <div class="rs-note">3 photos · fewer reviews than 78% of nearby competitors</div>
        </div>
      </div>

      <div class="report-diagnosis">
        <div class="rd-label">Diagnosis</div>
        <div class="rd-text">"Your business may not be struggling only because of poor marketing. The Financial District area shows declining lunch foot traffic and high restaurant saturation — these are structural pressures outside your control. However, your Google visibility, review count, and menu clarity are all meaningfully below the area average and are fixable within weeks."</div>
      </div>

      <div class="report-breakdown">
        <div class="rb-col">
          <div class="rb-heading"><span style="color:#22C55E;">●</span> Controllable factors</div>
          <div class="rb-row">
            <span class="rb-row-label">Google Visibility</span>
            <div class="rb-bar-group">
              <div class="rb-bar"><div class="rb-bar-fill" style="width:30%"></div></div>
              <span class="rb-score">30</span>
            </div>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Review Health</span>
            <div class="rb-bar-group">
              <div class="rb-bar"><div class="rb-bar-fill" style="width:55%"></div></div>
              <span class="rb-score">55</span>
            </div>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Menu Clarity</span>
            <div class="rb-bar-group">
              <div class="rb-bar"><div class="rb-bar-fill" style="width:40%"></div></div>
              <span class="rb-score">40</span>
            </div>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Website Quality</span>
            <div class="rb-bar-group">
              <div class="rb-bar"><div class="rb-bar-fill" style="width:35%"></div></div>
              <span class="rb-score">35</span>
            </div>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Social Presence</span>
            <div class="rb-bar-group">
              <div class="rb-bar"><div class="rb-bar-fill" style="width:20%"></div></div>
              <span class="rb-score">20</span>
            </div>
          </div>
        </div>
        <div class="rb-col">
          <div class="rb-heading"><span style="color:#EF4444;">●</span> External pressures</div>
          <div class="rb-row">
            <span class="rb-row-label">Competition Density</span>
            <span class="chip chip-high">High</span>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Local Demand</span>
            <span class="chip chip-high">Declining</span>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Consumer Spending</span>
            <span class="chip chip-med">Pressured</span>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Foot Traffic</span>
            <span class="chip chip-med">Below avg</span>
          </div>
          <div class="rb-row">
            <span class="rb-row-label">Cost Environment</span>
            <span class="chip chip-high">High</span>
          </div>
        </div>
      </div>

      <div class="report-strategy">
        <div style="font-size:20px;flex-shrink:0;padding-top:2px;">→</div>
        <div>
          <div class="rstr-label">Recommended strategy</div>
          <div class="rstr-title">Fix basics + Differentiate</div>
          <div class="rstr-desc">Your execution has clear, fixable gaps — but the market is also working against you. Improving visibility and menu basics could meaningfully help. Long-term, plan for loyal customer retention and distinct positioning to survive the area's high saturation.</div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- ─── ALGORITHM EXPLAINER ───────────────────────────────────────── -->
<section class="section algorithm" id="how">
  <div class="container">
    <div class="alg-header">
      <div class="label">How it works</div>
      <h2 class="section-title">Four analyses. One honest score.</h2>
      <p class="section-sub">We combine public signals, competitor comparison, and your inputs to separate what's fixable from what's structural.</p>
    </div>

    <div class="alg-grid">
      <div class="alg-card">
        <div class="alg-num">1</div>
        <div class="alg-title">Controllable Score</div>
        <div class="alg-desc">We score how well your business is executing on the factors you can actually change — visibility, reviews, menu design, website, and positioning.</div>
        <div class="alg-tags">
          <span class="alg-tag">Google photos</span>
          <span class="alg-tag">Review count</span>
          <span class="alg-tag">Menu clarity</span>
          <span class="alg-tag">Website</span>
          <span class="alg-tag">Opening hours</span>
        </div>
      </div>
      <div class="alg-card">
        <div class="alg-num">2</div>
        <div class="alg-title">External Pressure Score</div>
        <div class="alg-desc">We estimate the structural market pressure acting on your business — competitor density, local demand signals, consumer spending environment, and cost levels.</div>
        <div class="alg-tags">
          <span class="alg-tag">Nearby competitors</span>
          <span class="alg-tag">Foot traffic</span>
          <span class="alg-tag">Local demand</span>
          <span class="alg-tag">Cost pressure</span>
        </div>
      </div>
      <div class="alg-card">
        <div class="alg-num">3</div>
        <div class="alg-title">Fixability Score</div>
        <div class="alg-desc">We calculate what proportion of your challenges appear fixable through business actions, versus structural market conditions outside your control.</div>
        <div class="alg-tags">
          <span class="alg-tag">Controllable ÷ Total</span>
          <span class="alg-tag">Weighted formula</span>
          <span class="alg-tag">Displayed as %</span>
        </div>
      </div>
      <div class="alg-card">
        <div class="alg-num">4</div>
        <div class="alg-title">Competitor Benchmark</div>
        <div class="alg-desc">We compare your visibility, review count, rating, and online presence against similar businesses operating within walking distance of your location.</div>
        <div class="alg-tags">
          <span class="alg-tag">Rating percentile</span>
          <span class="alg-tag">Review volume</span>
          <span class="alg-tag">Photo count</span>
          <span class="alg-tag">Category match</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── PRICING ───────────────────────────────────────────────────── -->
<section class="section pricing" id="pricing">
  <div class="container">
    <div class="pricing-header">
      <div class="label">Pricing</div>
      <h2 class="section-title">Simple and transparent</h2>
      <p class="section-sub">Start with a free scan. Pay only when you want the full analysis.</p>
    </div>

    <div class="pricing-grid">

      <div class="price-card">
        <div class="plan-name">Free Scan</div>
        <div class="plan-desc">A high-level diagnosis: market pressure rating, fixability score, and your top 3 prioritised actions.</div>
        <div class="plan-price">
          <div class="price-amt">$0</div>
          <div class="price-per">No account required</div>
        </div>
        <ul class="plan-features">
          <li>Market Pressure rating</li>
          <li>Fixability Score</li>
          <li>3 prioritised actions</li>
          <li>Nearby competitor count</li>
          <li class="dim">Full factor breakdown</li>
          <li class="dim">Competitor benchmarks</li>
          <li class="dim">Strategy recommendation</li>
        </ul>
        <a href="/#analyze" class="btn btn-outline plan-cta">Run Free Scan</a>
      </div>

      <div class="price-card featured">
        <div class="featured-tag">Full analysis</div>
        <div class="plan-name">Diagnosis Report</div>
        <div class="plan-desc">The complete Reality Check — every factor scored, benchmarked, and explained in plain language.</div>
        <div class="plan-price">
          <div class="price-amt">$29</div>
          <div class="price-per">One-time · no subscription</div>
        </div>
        <ul class="plan-features">
          <li>Everything in Free Scan</li>
          <li>Full controllable factor breakdown</li>
          <li>External pressure analysis</li>
          <li>Competitor benchmark (10+ nearby)</li>
          <li>Strategy recommendation</li>
          <li>PDF export</li>
          <li class="dim">Monthly tracking</li>
        </ul>
        <a href="/#analyze" class="btn btn-primary plan-cta">Get Full Report</a>
      </div>

      <div class="price-card">
        <div class="plan-name">Monthly Monitoring</div>
        <div class="plan-desc">Track how your scores change month by month as you implement improvements and conditions shift.</div>
        <div class="plan-price">
          <div class="price-amt">$49</div>
          <div class="price-per">Per month · cancel anytime</div>
        </div>
        <ul class="plan-features">
          <li>Everything in Diagnosis Report</li>
          <li>Fresh report each month</li>
          <li>Score change tracking</li>
          <li>New competitor alerts</li>
          <li>Market condition updates</li>
          <li>Unlimited PDF exports</li>
          <li>Priority support</li>
        </ul>
        <a href="/#analyze" class="btn btn-outline plan-cta">Start Monitoring</a>
      </div>

    </div>
  </div>
</section>

<!-- ─── CTA BANNER ────────────────────────────────────────────────── -->
<section class="cta-banner">
  <div class="container">
    <span class="label">Honest · Practical · Free to start</span>
    <h2>Get a diagnosis before spending<br>more money on marketing.</h2>
    <p>Most small businesses spend on ads and promotions before understanding what is actually holding them back. ShopWise tells you what to fix first — and what to stop worrying about.</p>
    <div class="cta-actions">
      <a href="/#analyze" class="btn btn-white btn-lg">Run a Reality Check →</a>
      <a href="/sample/overview" class="btn btn-ghost-white btn-lg">View sample report</a>
    </div>
  </div>
</section>

<!-- ─── FOOTER ────────────────────────────────────────────────────── -->
<footer>
  <div class="container">
    <div class="footer-grid">
      <div>
        <div class="footer-brand">ShopWise</div>
        <p class="footer-desc">Honest business diagnostics for local restaurants, cafés, and independent shops. Built to separate what's fixable from what's structural.</p>
      </div>
      <div class="footer-col">
        <h4>Product</h4>
        <ul>
          <li><a href="#how">How it works</a></li>
          <li><a href="/sample/overview">Sample report</a></li>
          <li><a href="/pricing">Pricing</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h4>Company</h4>
        <ul>
          <li><a href="#">About</a></li>
          <li><a href="#">Contact</a></li>
          <li><a href="#">Privacy</a></li>
        </ul>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© 2026 ShopWise · All rights reserved</span>
    </div>
  </div>
</footer>

<script>
  const burger = document.getElementById('burgerBtn');
  const menu   = document.getElementById('mobileMenu');
  burger.addEventListener('click', () => menu.classList.toggle('open'));
  menu.querySelectorAll('a').forEach(a => a.addEventListener('click', () => menu.classList.remove('open')));
</script>

</body>
</html>
